# deps
node_modules/
.pnpm-store/

# build
dist/
.next/
out/
*.tsbuildinfo

# env — .env.example 만 커밋한다
.env
.env.*
!.env.example

# 로컬 인프라 데이터
.docker-data/

# 수집 캐시 · 로컬 스냅샷 (probe 가 저장하는 HTML/JSON 샘플)
.cache/
samples/
!samples/.gitkeep

# 로그
*.log
npm-debug.log*
pnpm-debug.log*

# 테스트 산출물
coverage/
test-results/
playwright-report/

# OS / editor
.DS_Store
.idea/
.vscode/
bash.exe.stackdump
# 데모 게시판의 살아있는 상태 — 글 하나 쓸 때마다 덮어써지므로 버전에 넣지 않는다.
# 없으면 server.mjs 가 내장 시드로 시작한다.
demo-board/board-data.json
# board-seed.json 은 반대로 **버전에 넣는다** — [기준 저장] 을 누를 때만 바뀌는
# 큐레이션한 기준점이고, 이게 저장소에 있어야 다른 VM 에서 pull 만으로 리허설을 반복할 수 있다.
