/**
 * 공유 뷰어 (D4) — 링크 하나로 설치 없이 브라우저에서 즉시 연주.
 *
 * TODO(D4): URL 의 악기 id 로 .inst 로드, 잼 세션 WebSocket 접속,
 *           남의 터치를 파문으로 표시, QR 진입.
 */
export default function ViewerPage() {
  return <p>공유 뷰어 — TODO(D4)</p>;
}
