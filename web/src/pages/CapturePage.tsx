/**
 * 채집 화면 (D2) — 폰 카메라/마이크로 물체를 5~10지점 톡톡 치며 녹음.
 *
 * TODO(D2): 촬영/녹음 UI, 업로드 → 처리 서버 → .inst 수신 → 연주 연결.
 * 타격 위치 자동 검출이 막히면 폴백: 사용자가 사진에서 직접 탭으로 지정.
 * TODO(D5): 채집 직후 악기 점수 표시 ("모드 7개, 지속 3.2초 — 훌륭한 악기예요").
 */
export default function CapturePage() {
  return <p>채집 화면 — TODO(D2)</p>;
}
