# CLAUDE.md — 이 저장소에서 일하는 규칙

**진실의 원천은 [`docs/기획서-v2.md`](./docs/기획서-v2.md) + [`docs/spec-delta-0727.md`](./docs/spec-delta-0727.md) 다.**
기획서는 고정 문서이고, 델타는 07-27 논의로 확정된 변경(뷰·작업실·능동성)이다 — **둘이 어긋나면 델타가 이긴다.**
이 파일은 그 문서들을 코드 작업에서 지킬 수 있게 압축한 것이다. 그 이후에 바뀐 기술 결정은 [`docs/adr.md`](./docs/adr.md) 에 있다.

## 상위 제약 셋 (기획서 5장 — 이것만은 조잡하면 안 된다)

1. **기능 ② 소스 접합** — 기존 스키마에 두 번째 사이트를 붙이는 것. 이 제품이 다른 제품과 갈라지는 유일한 지점.
   여기서 사용자가 할 일이 많아지면 아무도 두 번째 소스를 붙이지 않고, 그러면 이 제품을 쓸 이유가 사라진다.
2. **기능 ④ 자가 치유** — 깨진 걸 감지·격리·재컴파일·승격. 그리고 **복구를 로그로 쌓아 보여주는 것**까지가 기능이다.
   조용히 고치면 사용자는 이 제품이 무엇을 해주는지 영원히 모른다.
3. **보장선 B1~B7** (기획서 4-1 · [docs/guardrails.md](./docs/guardrails.md)) — 화면·문구에 관한 모든 판단의 상위 제약.
   하나라도 깨지면 그 기능은 미완성이다.

나머지(①스키마 추론 ③타입 정규화 ⑤네 표면)는 리소스가 부족하면 조잡해도 된다.

델타가 여기에 **v1 필수 셋**을 추가했다 (델타 14절 — 이 셋 없이 나머지를 만들면 다시 짜야 한다):
**뷰를 일급 객체로 저장 · `enter` 전이 감지 · 뷰 건강 상태.**

## 하지 말 것

- **사용자 노출 문자열에 내부 명사를 쓰지 마라** (B2). 어댑터·런·스펙·드리프트·파서·probe·컴파일 전부 금지.
  사용자가 배우는 명사는 "컬렉션" 하나다. 소스는 **"사이트"** 또는 **"출처"**, 런은 **"수집 기록"**, 아이템은 **"항목"**.
  상태 값의 내부 키(`needs_attention`)를 화면에 그대로 찍지 마라.
- **셀렉터를 입력받는 UI 를 만들지 마라** (B1). CSS·XPath·정규식·JSONPath 입력칸은 "고급 설정" 안에 숨겨도 위반이다.
  못 찾은 필드는 **값을 붙여넣게** 하고 시스템이 역추적한다.
- **LLM 을 런타임 경로에 넣지 마라** (원칙 ① · ADR A1). 정기 수집·API 응답·MCP 도구 실행에 LLM 호출이 하나라도 들어가면 안 된다.
  LLM 은 어댑터를 **만들 때와 고칠 때만** 부른다. 사용자가 API 를 만 번 불러도 Gemini 호출은 0이다.
- **어댑터에 코드를 실행시키지 마라** (원칙 ② · ADR A2). `eval`·`new Function`·`vm` 금지.
  LLM 이 만드는 것은 코드가 아니라 JSON 스펙이고, `packages/core` 의 해석기가 그걸 읽어 돈다.
- **변환 연산자 집합 밖의 표현을 임시방편으로 끼워넣지 마라** (기획서 11장).
  표현이 부족하면 **연산자를 추가한다.** `op: "custom"` 이나 문자열로 받은 표현식을 평가하는 길을 열지 마라.
  이 선이 무너지면 샌드박스가 필요해지고, 그 순간 5일 안에 끝나지 않는다.
- **뷰 술어(op)도 같은 규율이다** (델타 2-7). 닫힌 집합 밖 술어·자유 표현식·SQL 조각을 열지 마라 —
  뷰는 표·알림·REST·MCP 네 출구를 전부 타므로 한 번 열면 네 곳에서 안전을 보장해야 한다.
- **추천·제안 기능을 만들지 마라** (델타 4-4). "이런 사이트도 추가해보세요"류는 정의권 침범이다.
  올바른 능동성은 **정의는 사용자가, 관찰은 시스템이** — 침묵 감지·상태 줄처럼 관찰만 말한다.
- **뷰 조건 값은 상대 표현으로 저장한다** (델타 2-6). `this_month`/`d7` 이지 `2026-08-31` 이 아니다 —
  절대값으로 저장된 뷰는 한 달 뒤 조용히 죽는다.
- **Run 이력·항목 스냅샷을 지우는 코드를 쓰지 마라** (델타 10절). 시간축 데이터는 나중에 켤 스위치다.
- **빨간 에러를 화면에 던지지 마라** (B4). HTTP 코드·스택 트레이스 노출 금지. 실패는 상태 문구다.
- **기획서(`docs/기획서-v2.md`)를 고치지 마라.** 고정 문서다. 기술 변경은 `docs/adr.md` 부터다.

## 코드 규약

`tsconfig.base.json` 이 강제한다. 어기면 typecheck 가 깨진다.

- **ESM 전용** (`"type": "module"`). CommonJS 문법 금지.
- `verbatimModuleSyntax` → 타입만 쓰는 import 는 반드시 **`import type { X } from '...'`**.
- `noUncheckedIndexedAccess` → `arr[0]` 의 타입은 `T | undefined`. 인덱싱 후 반드시 좁혀라.
- `isolatedModules` → 재export 는 `export { type X }` 형태. `const enum` 금지.
- `moduleResolution: "bundler"` → 상대 import 에 `.js` 확장자를 **붙이지 않는다** (`./foo` 그대로).
- `noEmit` → 어떤 패키지도 `dist/` 를 만들지 않는다.
- **워크스페이스 간 import 는 항상 패키지 이름으로.** `import type { Collection } from '@endpointer/core'`.
  `../../packages/core` 로 타고 넘어가지 마라.
- **`packages/core` 는 빌드가 없다** (ADR A15). 소스가 그대로 소비된다. 고치면 즉시 세 앱에 반영된다 —
  그래서 core 를 고치는 건 남의 트랙을 고치는 것과 같다.
- **식별자·타입·파일명은 영어. 주석과 사용자 노출 문자열은 한국어.**
- core 는 브라우저에서도 돌 수 있어야 한다. cheerio 를 import 하지 말고 `HtmlAdapter` 로 주입받아라 (ADR A18).
- 새 의존성을 추가하기 전에 [`docs/adr.md`](./docs/adr.md) 를 보고, 없는 결정이면 ADR 부터 적어라.

## 트랙 경계 (기획서 14장)

두 사람이 같은 파일을 만지지 않게 **트랙 경계를 디렉터리 경계와 일치시킨다.**

| 경로 | 트랙 | 규칙 |
|---|---|---|
| `apps/worker` | **A — 파이프라인** | probe · 어댑터 컴파일 · 수집 · 검증 · 자가 치유 · 스케줄러 · 구독 발송 |
| `apps/web`, `apps/mcp` | **B — 표면** | 인증 · 화면 · 표 · 피드 · REST API · MCP |
| `packages/core` | **공유** | G0 이후 변경 시 **상호 통보.** 조용히 고치면 상대 트랙이 조용히 깨진다 |
| `docs/`, 루트 설정 | 공동 | |

**G0 에서 고정하고 이후 바꾸지 않는 것** (양쪽이 같이 있을 때만 바꾼다):
core 의 타입 정의 · DB 스키마와 마이그레이션 · `GET /api/v1/{slug}` 응답 형태 · 시드가 만드는 가짜 데이터 · 상태 값 집합.

## 자주 쓰는 명령

루트에서 실행한다. (`package.json` 의 스크립트 이름 그대로다)

```bash
pnpm infra:up                # postgres + redis 컨테이너 기동
pnpm db:generate             # 스키마 변경 후 마이그레이션 SQL 생성
pnpm db:migrate              # 마이그레이션 적용
pnpm db:seed                 # 가짜 데이터 (멱등 — 두 번 돌려도 안전)
pnpm db:reset                # 볼륨까지 날리고 migrate + seed
pnpm dev                     # 세 앱 병렬 (web 3000 · worker · mcp 3002)
pnpm dev:web                 # 하나만 띄우기
pnpm typecheck               # 전 패키지. 커밋 전 필수
pnpm test                    # 전 패키지 vitest
```

한 패키지만: `pnpm --filter @endpointer/core test`

## 기획서를 고치는 규칙 (기획서 0장)

| 파트 | 내용 | 수명 |
|---|---|---|
| Part I (1~6장) | 무엇을 왜 만드는가 | **고정.** 여기를 고쳐야 하면 그건 제품 변경이고 v3 다 |
| Part II (7~12장) | 어떻게 만드는가 | **교체 가능.** 조건이 추가되면 여기와 ADR 만 고친다 |
| Part III (13~16장) | 5일을 어떻게 굴리는가 | 관문 판정 결과에 따라 갱신 |

기술 변경 절차는 **ADR 이 먼저다**:
`docs/adr.md` 에서 해당 결정을 찾는다 → "다시 보게 만드는 조건"에 해당하는지 본다 →
해당하면 상태를 `superseded` 로 바꾸고 새 번호를 추가한다 → "영향받는 곳"에 적힌 곳만 고친다.
**코드를 먼저 바꾸고 ADR 을 나중에 적지 마라.** 그 순서로는 상대 트랙이 변경을 모른다.

## 관련 문서

- [`docs/기획서-v2.md`](./docs/기획서-v2.md) — 진실의 원천 (수정 금지)
- [`docs/gates.md`](./docs/gates.md) — G0~G5 판정 체크리스트
- [`docs/guardrails.md`](./docs/guardrails.md) — B1~B7 점검 절차
- [`docs/adr.md`](./docs/adr.md) — 기술 결정과 교체 조건
- [`README.md`](./README.md) — 처음 클론한 사람용
